{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-window",
  "description": "No plugin permissions. Fixed loopback probes, validated manual project-path binding, and five argument-free Rust launcher actions only.",
  "windows": ["main"],
  "permissions": []
}
